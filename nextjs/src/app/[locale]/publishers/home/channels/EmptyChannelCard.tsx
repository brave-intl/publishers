'use client';

import Button from '@brave/leo/react/button';
import { useTranslations } from 'next-intl';

export default function EmptyChannelCard({ }) {
  const t = useTranslations();

  return (
    <div>
    </div>
  );
}
